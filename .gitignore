# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# One-time data scripts (may contain env vars / secrets)
scripts/update-pages.mjs
scripts/update-pages2.mjs
scripts/create-test-page.mjs

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*.local

# Agent runtime files (logs, state, learnings contain runtime data)
/logs/
agents/state/
agents/memory/

# Claude Code settings (commands are committed, settings are not)
.claude/settings.json
.claude/settings.local.json

# GitHub token
.gh-token

# MCP config (contains API keys)
.mcp.json
SETUP.md
RESEND-SETUP.md
context.txt
# Security
tmp/mcp-configs/
.mcp.json
*.credentials.json
instagram-tokens.json
instagram-accounts.json
bridge-pointer.json

# Mulch knowledge store (data file, not config)
.mulch/learnings.jsonl
.mulch/.lock
tmp/

# Large video files — too large for GitHub (use LFS or external storage)
assets/video/*.mp4
assets/video/*.mov
assets/video/*.avi
assets/video/th-2/

# Large binary assets — stored in Supabase Storage
public/videos/
scripts/*.mp4
scripts/*.mov
assets/video/*.png
assets/video/*.webm
